[build]
  base = "artifacts/mahi-birthday"
  command = "corepack pnpm install --no-frozen-lockfile && corepack pnpm build"
  publish = "artifacts/mahi-birthday/dist"

[build.environment]
  NODE_VERSION = "24"
